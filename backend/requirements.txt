# === Core Framework ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# === Database ===
chromadb==0.6.3
# ChromaDB 0.6.x still uses the pre-6.0 capture() signature.
posthog<6.0.0

# === Document Processing ===
pymupdf==1.25.1

# === AI / Embeddings / LLM ===
openai==1.59.6

# === Utilities ===
pydantic==2.10.4
pydantic-settings==2.7.1

# Local embeddings and reranking are intentionally isolated in
# requirements-local.txt because they install the much larger PyTorch stack.
